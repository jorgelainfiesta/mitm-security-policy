import { htmlSafe } from '../../../helpers/html-safe';
import { module, test } from 'qunit';

module('Unit | Helper | html safe');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = htmlSafe(42);
  assert.ok(result);
});
