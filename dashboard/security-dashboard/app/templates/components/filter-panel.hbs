{{yield}}
{{#paper-list}}
    {{#each tags as |tag|}}
        {{paper-divider}}
        {{#paper-radio value=tag.id selected=selectedValue}} {{tag.description}} {{/paper-radio}}
    {{/each}}
{{/paper-list}}